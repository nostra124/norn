[package]
name = "norn-sys"
version = "0.1.0"
edition = "2021"
links = "norn"
description = "Raw FFI bindings to libnorn (the C SDK)"
license = "MIT OR Apache-2.0"
build = "build.rs"

[lib]
doctest = false
